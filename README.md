# Анализ транзакций почти на все случаи жизни

## Скрипт `calc_stats.py`

## Требования

- Git
- Python 3.6+
- Установленные зависимости из `requirements.txt`

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/bah223/calc_stats.git
   cd calc_stats
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   
   Или установите вручную:
   ```bash
   pip install pandas openpyxl
   ```

## Быстрый старт под Windows

Если вы работаете в Windows и не хотите возиться с командной строкой:

```cmd
run_scripts.bat
```

Бат-файл автоматически:
- проверит, установлены ли зависимости (`pandas`, `openpyxl`);
- при необходимости установит/обновит их через `pip`;
- предложит выбрать, какой из скриптов запустить:
  1. `angelina_report.py`
  2. `calc_stats.py`
  3. `12oo.py`
  4. 'mosteh.py'
  5. 'rep0000.py'

После выполнения выбранного скрипта окно останется открытым, чтобы вы могли увидеть результат.

---

## Использование

1. Поместите Excel-файлы с транзакциями в папку со скриптом.
2. Запустите скрипт:
   ```bash
   python calc_stats.py
   ```

## Формат файлов

Скрипт ищет файлы по шаблону: `Transaction-List-Date_*.xlsx`

Ожидаемая структура файла:
- Колонка 6 (индекс 5): статус транзакции
- Колонка 7 (индекс 6): сумма в RUB

## Вывод

Скрипт выводит:
- Общее количество операций
- Количество успешных операций (со статусом 'CAPTURED')
- Процент успешных операций
- Оборот за сутки
```
=== Результаты анализа ===
Всего операций: 7100
Успешных операций: 6200
Success Rate: 87.32%
Оборот за сутки: 6,597,305.43 RUB
```
## Скрипт `angelina_report.py`

Этот скрипт формирует расширенный отчёт по каждому файлу:

- Подсчитывает количество и сумму транзакций для статусов:
  - `CAPTURED`
  - `CANCELLED`
  - `DECLINED`
  - `REFUNDED`
- Выводит сводку по каждому файлу отдельно.

### Запуск

```bash
python angelina_report.py
```

### Что вы увидите
```
=== Результаты анализа ===
- Успешных транзакций (CAPTURED): 479 шт на сумму 7 744 886,92 RUB
- Неоплаченных транзакций (CANCELLED): 202 шт на сумму 7 055 557,00 RUB
- Отклоненных транзакций (DECLINED): 0 шт на сумму 0,00 RUB
- Возвраты (REFUNDED): 7 шт на сумму 34 700,00 RUB

✅ Обработка завершена.
```

## Скрипт `12oo.py`

Скрипт анализирует Excel-файлы с транзакциями и подсчитывает количество транзакций, совершенных до 12:00 по московскому времени.

### Особенности:
- Автоматически находит все файлы по шаблону `Transaction-List-Date_*.xlsx`
- Учитывает московский часовой пояс (UTC+3)
- Пропускает некорректные записи
- Выводит детальную информацию по каждому файлу
- **Интерактивный выбор колонки времени** (Created, Last Updated, Completion Date и т.д.)
- Группировка по мерчантам

### Запуск

```bash
python 12oo.py
```

При запуске скрипт предложит выбрать, по какой колонке считать транзакции:
```
🕐 Найдены временные колонки:
   1. [1] Created
   2. [3] Last Updated
   3. [4] Completion Date

Выбери по какой колонке считать транзакции до 12:00:
Введи номер (1-3): 
```

### Пример вывода:
```
📁 Найдено файлов: 2
 - Transaction-List-Date_2026-01-19T12_55_00.xlsx
 - Transaction-List-Date_2026-01-19T12_55_13.xlsx

✅ Transaction-List-Date_2026-01-19T12_55_00.xlsx
   Всего строк: 2000 | До 12:00: 3 | Ошибок время: 298
   Мерчанты: Pagsmile Limited: 3

✅ Transaction-List-Date_2026-01-19T12_55_13.xlsx
   Всего строк: 2000 | До 12:00: 242 | Ошибок время: 294
   Мерчанты: Pagsmile Limited: 242

==================================================
📊 Всего транзакций до 12:00 по Москве: 245
Список мерчантов и их транзакций до 12:00:
- Pagsmile Limited — 245
==================================================
```

### Debug режим

Для анализа проблем можно включить DEBUG режим. Откройте файл `12oo.py` и измените:
```python
DEBUG = False  # на True
```

Это покажет структуру файлов, найденные колонки и примеры преобразования времени.

---

## Скрипт `mosteh.py`

Скрипт для формирования детального отчета о транзакциях МосТех с фильтрацией по датам.

### Особенности:
- Фильтрация по дате начала и конца периода
- Профессиональное форматирование Excel-файла
- Автоматическое определение ширины колонок
- Применение стилей и цветного выделения
- Поддержка больших объемов данных

### Запуск

```bash
python mosteh.py --start_date 2025-01-15 --end_date 2025-01-15
```

### Параметры

- `--start_date YYYY-MM-DD` - дата начала фильтрации (обязательно)
- `--end_date YYYY-MM-DD` - дата конца фильтрации (обязательно)

### Примеры

#### Отчет за конкретный день:
```bash
python mosteh.py --start_date 2026-01-19 --end_date 2026-01-19
```

#### Отчет за неделю:
```bash
python mosteh.py --start_date 2026-01-15 --end_date 2026-01-19
```

#### Через меню (интерактивно):
```bash
run_scripts.bat
# Выбираете опцию 4 - mosteh.py
# Вводите даты вручную
```

### Вывод

Скрипт генерирует файл `mosteh_[начало]-[конец].xlsx` с отчетом, содержащим:
- Полную информацию по транзакциям МосТех
- Детали по каждому платежу
- Отформатированные таблицы для удобства анализа


---

## Скрипт `rep0000.py`

Интерактивный скрипт для анализа данных по выбранным мерчантам. Позволяет выбрать 5 мерчантов вручную и получить полную аналитику по ним.

### Запуск

```bash
python rep0000.py
```

### Как это работает

При запуске скрипт предложит вам ввести данные:
```
Введите 1-й ID/название аккаунта: AA123456789
Введите 2-й ID/название аккаунта: AA987654321
...
Введите 5-й ID/название аккаунта: AA111222333
```

### Пример вывода

```
=== Анализ по выбранным аккаунтам ===

Аккаунт: AA195783946319400960 (Carusell/WhiteBird)
Базовый период: 150 транзакций
Текущий период: 180 транзакций
Изменение: рост на 20% (+30 транзакций)

Аккаунт: AA257439891205799936 (Pagsmile Limited)
Базовый период: 4723 транзакции
Текущий период: 2015 транзакций
Изменение: сокращение на 57.32% (-2708 транзакций)

...и так по каждому аккаунту
```
---

## Быстрые команды

### Все скрипты через меню (Windows):
```bash
cd Script
run_scripts.bat
```

### Запуск конкретного скрипта:
```bash
python 12oo.py                                           # До 12:00
python angelina_report.py                                # Отчет по статусам
python calc_stats.py                                     # Общая статистика
python mosteh.py --start_date 2026-01-19 --end_date 2026-01-19  # МосТех
python rep0000.py                                        # Интерактивный анализ
```

---
